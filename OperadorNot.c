#include <stdio.h>

int main() {
  int a = 10;

  if (!(a > 0)) {
    printf("a não é um número positivo\n");
  } else {
    if (!(a < 0)) {
    printf ("a nao e um numero negativo\n");
  }
  }
  return 0;
}